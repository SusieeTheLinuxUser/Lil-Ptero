# DRAFT F-Droid build recipe for Pterodactyl Mobile (Lil-Ptero).
#
# This file is NOT submitted anywhere yet — it's a working copy kept in this
# repo for reference. To actually submit: fork https://gitlab.com/fdroid/fdroiddata,
# copy this file (comments stripped — F-Droid asks for that) to
# metadata/dev.susiee.lilptero.yml in that fork, test it locally with
# fdroidserver, then open a merge request.
# See fdroid/README.md in this repo for the full step-by-step process.
#
# Modeled on a real, currently-building Flutter recipe already in fdroiddata
# (com.iakmds.librecamera.yml — "Libre Camera") rather than guessed syntax.
# Simplified from an earlier draft of this file that copied the official
# template's advanced reproducible-build/upstream-path machinery, which
# LibreCamera's real (accepted, building) recipe doesn't actually need for
# a basic submission.

Categories:
  - Internet
License: MIT
AuthorName: SusieeTheLinuxUser
SourceCode: https://github.com/SusieeTheLinuxUser/Lil-Ptero
IssueTracker: https://github.com/SusieeTheLinuxUser/Lil-Ptero/issues
Changelog: https://github.com/SusieeTheLinuxUser/Lil-Ptero/releases

AutoName: Pterodactyl Mobile
RepoType: git
Repo: https://github.com/SusieeTheLinuxUser/Lil-Ptero.git

Builds:
  - versionName: '0.1.0'
    versionCode: 1
    commit: 88335637632149d32e23f846df1aa8062df36c20 # tag v0.1.0
    output: build/app/outputs/flutter-apk/app-release.apk
    # Pinned to match .github/workflows/release.yml's flutter-version — keep
    # these in sync when bumping Flutter.
    srclibs:
      - flutter@3.47.4
    build:
      - $$flutter$$/bin/flutter config --no-analytics
      - $$flutter$$/bin/flutter pub get --enforce-lockfile
      - $$flutter$$/bin/flutter build apk --release
    scanignore:
      # Downloaded Dart/Flutter tooling cache, not part of our source.
      - $$flutter$$/bin/cache

AutoUpdateMode: Version
UpdateCheckMode: Tags
UpdateCheckData: pubspec.yaml|version:\s.+\+(\d+)|.|version:\s(.+)\+
CurrentVersion: '0.1.0'
CurrentVersionCode: 1
